# AGENTS.md

## IDE 및 도구 사용 원칙

이 저장소의 기본 IDE는 JetBrains Rider이다.

Rider MCP를 사용할 수 있는 경우, C#/.NET 코드에 대한 의미 기반 작업은 일반적인 텍스트 검색이나 수동 편집보다 Rider의 IDE 기능을 우선해서 사용한다.

특히 다음 작업에서는 Rider MCP를 우선한다.

* 심볼 검색 및 코드 탐색
* 심볼의 사용처 및 참조 찾기
* 타입, 메서드, 클래스 등의 심볼 정보 확인
* 이름 변경 및 기타 리팩터링
* 코드 검사, 경고 및 오류 확인
* 프로젝트 및 솔루션 구조 확인
* Rider의 Run Configuration을 이용한 실행 및 테스트

코드 구조와 의미에 영향을 주는 변경은 단순 문자열 치환보다 Rider의 리팩터링 기능을 우선한다.

Rider MCP를 사용할 수 없거나 필요한 기능을 제공하지 않는 경우에는 셸, 파일 시스템, 검색 도구 등 다른 방법으로 자연스럽게 대체한다.

C# 코드를 수정한 후에는 가능한 경우 Rider의 코드 검사 기능을 사용해 수정한 파일의 오류, 경고 및 잠재적인 문제를 확인한다.

## 작업 원칙

CodeMap 질의 기능을 추가하거나 수정할 때는 SQL 저장소 경로와 메모리 스냅샷
경로가 동일한 결과를 내는지 기존 parity 테스트 또는 새 테스트를 함께 갱신한다.

변경하기 전에 관련 코드와 기존 구현 방식을 먼저 확인한다.

불필요하게 큰 범위의 변경을 하지 않고, 요청한 작업을 해결하는 데 필요한 최소 범위만 수정한다.

기존 프로젝트의 코드 스타일, 네이밍 규칙, 디렉터리 구조 및 아키텍처를 따른다.

새로운 구현을 추가하기 전에 프로젝트 내부에 이미 비슷한 패턴이나 공통 기능이 있는지 확인하고 가능하면 기존 방식을 재사용한다.

리팩터링을 수행할 때는 기능 동작을 의도치 않게 변경하지 않도록 한다.

작업 완료 후에는 가능한 범위에서 빌드, 테스트, 코드 검사 등을 통해 변경 사항을 검증한다.
