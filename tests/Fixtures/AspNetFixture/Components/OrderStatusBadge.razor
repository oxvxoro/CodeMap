@* No code-behind (.razor.cs) file — exercises Renders resolving to a sibling Razor
   artifact node purely from the predeclare pass, with no C# class to fall back to
   (plan §6, §10.1 Renders_CodeBehindlessComponent_IsResolvedAsArtifact). *@
<span class="order-status-badge">pending</span>
