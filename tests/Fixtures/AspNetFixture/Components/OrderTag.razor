@* Ambiguous-name fixture half 1 of 2 (plan §6.4, §10.1
   AmbiguousComponentName_ProducesNoRenders): another RazorComponent artifact shares this
   file's simple name (OrderTag) via Components/Alt/OrderTag.razor, so <OrderTag /> below
   must resolve to neither. *@
<span class="order-tag">tag</span>
